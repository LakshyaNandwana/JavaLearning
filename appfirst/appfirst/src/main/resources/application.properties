#Exposing end points with * or with comma-delimited list
management.endpoints.web.exposure.include=*
#management.endpoints.web.exposure.include=health,info
management.info.env.enabled=true

#EXCLUDE individual endooints with comma-delimeted list
management.endpoints.web.exposure.exclude=health,info 

info.app.name=AppFirst
info.app.description= Good description
info.app.version=1.0.0

##Defining custom properties and using it in the code
coach.name=ABC @XYZ
team.name=qwerty


#Change Spring boot embedded server port
server.port=7070

#Set the context path of the application
#all requests should be prefixed with /appfirst
server.servlet.context-path=/appfirst